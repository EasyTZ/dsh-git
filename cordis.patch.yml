# dsh-git 的 bundle 层：让 `dsh plugin add dsh-git` 装完就能用。没有它，dsh 的
# plugin 命令只打印
#   warning: dsh-git declares no dsh.bundle — installed as a plain dependency
# 然后把包装进去、**永远不激活**，用户得自己手写 patch 才能看到面板。

- insert:
    - id: dsdesktop-git
      name: '@easytz/dsh-git'
